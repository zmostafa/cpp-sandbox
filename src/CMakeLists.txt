option(CPP_SANDBOX_TARGETNAME"Enable compilation of TARGETNAME" ON)

# SOLID Principles
if(CPP_SANDBOX_TARGETNAME)
  message("")
  add_subdirectory()
  # add_subdirectory()
  # add_subdirectory()
endif()
